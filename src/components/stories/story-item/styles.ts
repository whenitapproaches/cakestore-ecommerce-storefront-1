"use client";

import styled from "@mui/material/styles/styled";

export const StyledRoot = styled("div")({
  outline: 0,
  display: "flex",
  borderRadius: 4,
  cursor: "pointer",
  overflow: "hidden"
});
